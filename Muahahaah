-- Simple function setup (execute loadstring once)
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local remote = ReplicatedStorage.BillboardGui.EnableStatisticsGui

-- Create the Add function globally
function _G.Add(path)
    remote:FireServer(path)
end

print("Add function loaded! Usage: Add(your_path_here)")
