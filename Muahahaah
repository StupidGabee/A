-- Add function with error checking
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- Try to find the remote
local success, remote = pcall(function()
    return ReplicatedStorage.BillboardGui.EnableStatisticsGui
end)

if success and remote then
    -- Create the Add function
    function _G.Add(path)
        remote:FireServer(path)
        print("Sent:", path)
    end
    print("✅ Add function loaded successfully!")
else
    print("❌
